# GoRush Customer Platform

This directory contains the independent Customer Platform artifacts:
- `frontend/`: The Flutter application.
- `backend/`: The NestJS Node.js backend acting as the Customer API.

## Running the Application
### Frontend (Flutter)
```bash
cd frontend
flutter pub get
flutter run
```

### Backend (NestJS)
```bash
cd backend
npm install
npm run start:dev
```

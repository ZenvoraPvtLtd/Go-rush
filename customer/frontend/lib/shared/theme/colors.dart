import 'package:flutter/material.dart';

class GoRushColors {
  // Brand colors
  static const Color primary = Color(0xFF0F172A); // Deep slate/black, premium feel
  static const Color primaryLight = Color(0xFF1E293B);
  static const Color accent = Color(0xFF3B82F6); // Trustworthy blue for actions

  // Background and Surfaces
  static const Color background = Color(0xFFF8FAFC); // Very light grayish blue
  static const Color surface = Color(0xFFFFFFFF); // Pure white cards
  static const Color surfaceElevated = Color(0xFFF1F5F9);

  // Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);

  // Semantic
  static const Color border = Color(0xFFE2E8F0);
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);
  static const Color safety = Color(0xFFDC2626); // Strong red for safety SOS features
  
  static const Color overlay = Color(0x660F172A);

  // Aliases for compatibility
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F5F9);
  static const Color onSurfaceVariant = Color(0xFF475569);
  static const Color primaryContainer = Color(0xFFE2E8F0);
  static const Color onPrimaryContainer = Color(0xFF0F172A);
  static const Color onSurface = Color(0xFF0F172A);
}

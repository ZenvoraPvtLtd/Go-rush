# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration (PostgreSQL / PostGIS - Docker default)
DATABASE_URL="postgresql://gorush_admin:gorush_password@localhost:5432/gorush_backend?schema=public"

# Authentication (JWT Secret Key)
JWT_SECRET="gorush_super_secret_jwt_key_2026_dev"

# Redis Configuration (BullMQ & Cache)
REDIS_HOST="localhost"
REDIS_PORT=6379

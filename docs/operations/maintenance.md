# Maintenance Tracking

## Scope
Maintains logs of service records. Currently manual entry via Admin Control Tower. 
IoT Telemetry (live odometer, DTC codes) is **NOT CONFIGURED** and will require future integration.

## Models
`MaintenanceRecord` containing description and `performedAt`.
